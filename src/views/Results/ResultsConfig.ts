import Environment from '@/services/EnvironmentService';

export const resultsUrl: string = `${Environment.any.api}/results`;
export const resultsStatesUrl: string = `${Environment.any.api}/result_states`;
